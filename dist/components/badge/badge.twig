<span class="badge {{ type ? 'bg-' ~ type ~ ' text-bg-' ~ type : 'bg-primary text-bg-primary' }}{% if badge_classes|length > 0 %}{{ badge_classes|join(' ') }}"{% endif %}{{ pill ? 'rounded-pill' }}">
  {{- label -}}
</span>
