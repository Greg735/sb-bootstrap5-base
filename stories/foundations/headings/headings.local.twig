{% for heading_level in 1..6 %}
<h{{ heading_level }}>H{{ heading_level }} example of a title with Hx.</h{{ heading_level }}>
{% endfor %}

<hr>
H1 example with hx classes.

{% for heading_level in 1..6 %}
<h1 class="h{{ heading_level }}">H1 example with class "h{{ heading_level }}".</h{{ heading_level }}>
{% endfor %}

<hr>
H2 example with hx classes.

{% for heading_level in 1..6 %}
<h2 class="h{{ heading_level }}">H2 example with class "h{{ heading_level }}".</h{{ heading_level }}>
{% endfor %}

<hr>
Etc.
