<div class="content-wysiwyg mb-3{% if content_classes|length > 0 %} {{ content_classes|join(' ') }}{% endif %}"{% for attribute, value in content_wysiwyg_attributes %} {{ attribute }}="{{ value }}"{% endfor %}>{{ content }}</div>
