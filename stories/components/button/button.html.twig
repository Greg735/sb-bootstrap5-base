{% set size = (size == "default") ? null : size %}
{% set outline = outlined ? 'outline-' : '' %}
{% set full_width = full_width ? 'w-100' : '' %}

<button type="button" 
  class="btn 
    {{ type ? 'btn-' ~ outline ~ type : 'btn-'~ outline ~ 'primary' }}
    {{ size ? 'btn-' ~ size }}
    {{ full_width }}
  "
  {{ disabled ? 'disabled' }}
  >{{- label -}}</button>