<div class="{% if classes|length > 0 %} {{ classes|join(' ') }}{% endif %} image-zoom"{% for attribute, value in image_zoom_attributes %} {{ attribute }}="{{ value }}"{% endfor %}>
  {% include "@components/image/image.twig" with {
    src: src,
    alt: alt,
    width: width,
    height: height,
  } %}
</div>
