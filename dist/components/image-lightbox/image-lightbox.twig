<a href="{{ lightbox_dest_image_src }}" class="glightbox"  data-glightbox="{{ lightbox_caption }}"{% for attribute, value in attributes %} {{ attribute }}="{{ value }}"{% endfor %}>
  {% include "@components/image/image.twig" with {
    src: src,
    alt: alt,
    title: title,
    width: width,
    height: height,
    image_attributes: image_attributes
  } %}
</a>
